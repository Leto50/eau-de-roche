# Ces valeurs sont écrites automatiquement par `convex dev` en local et par
# `convex deploy --cmd` pendant le build Netlify.
CONVEX_DEPLOYMENT=
VITE_CONVEX_URL=
VITE_CONVEX_SITE_URL=

# Variables à définir sur le déploiement avec `convex env set`.
SITE_URL=http://localhost:3000
BETTER_AUTH_SECRET=
INITIAL_ADMIN_EMAIL=
INITIAL_ADMIN_PASSWORD=
SEED_SECRET=
